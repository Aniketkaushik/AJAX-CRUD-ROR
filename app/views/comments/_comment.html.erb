<% if comment.user == current_user %>
    
    <div class="divTableCell">
        <%= comment.content %>
    </div>

    <div class="divTableCell">
        <%= link_to 'Edit', edit_comment_path(comment), remote: true, class:"btn btn-outline-info btn-sm" %>
    </div>

<% end %>